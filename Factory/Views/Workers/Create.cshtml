@{
  Layout = "_Layout";
}

@model Factory.Models.Worker

<h4>Add Employee: </h4>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name, new {required = "required"})


  <input type="submit" value="Add new employee" class="btn btn-default" />
}